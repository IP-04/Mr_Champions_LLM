[phases.setup]
nixPkgs = ["python311", "gcc", "g++", "cmake", "libgomp"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install -r requirements.txt --no-cache-dir"
]

[start]
cmd = "uvicorn serve:app --host 0.0.0.0 --port $PORT"

